version: "3.9"

services:
  vchat:
    container_name: vchat-app
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.local
    ports:
      - "3000:3000"
    restart: unless-stopped
